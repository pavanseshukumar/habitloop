/** Layout constants shared across screens. */
export const layout = {
  screenPaddingX: 24,
  screenPaddingY: 24,
  contentMaxWidth: 520,
  hairline: 1,
  // Minimum comfortable tap target.
  touchTarget: 48,
};
